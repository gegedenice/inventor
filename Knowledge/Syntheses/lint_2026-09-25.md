# Lint — reindex + health-check — 2026-09-25

## Réconciliation de l'index

**4 entrées ajoutées (+ ce rapport), 0 retirée.** Deux passes non reportées depuis le lint 2026-09-13 :
la passe **lab** du 13-09 (dont le lint du même jour avait précédé les sorties) et l'ingest **decision models** du 25-09.

- **Inspirations** : `llm.md` — **« System One » / decision models** (Jev, AnyJev, GLiNER2.5-Decide).
- **Idées** : **applied_ideas_2026-09-13.md** (5 optimisations appliquées, lentille lab).
- **Expériences** : **exp_turboquant_kvcache_notices.md** (lab) ; **exp_decision_model_routing_biblio.md** (ingest).

Aucune entrée orpheline. Pas de Paper ajouté (les 3 sources decision-models sont des pointeurs, pas de fulltext).

## Health-check

Constats :

1. **Dérive = décalage lint/lab, pas oubli** : le lint 13-09 a tourné *avant* la passe lab du même jour, d'où 2 fichiers (applied_ideas + exp_turboquant) restés hors index jusqu'ici. Règle pratique : lancer `inventor-lint` **après** une passe lab, pas avant.
2. **Un cluster « sortie typée + confiance calibrée + escalade sous seuil » se cristallise** : Needle 2 (génération d'appels d'outils contrainte, confidence-gated) et les decision models (classification/décision calibrée non-générative) sont deux faces du même motif ; s'y rattachent Deja (score seuillable sans IA) et l'extraction (exp_needle, exp_decision_model). Candidat sérieux à une **synthèse transversale** (« décider, pas bavarder : contrat de type + calibration + seuil »).
3. **Nouvelle entrée bien reliée** : System One croise Needle 2, SLM propositions atomiques, Deja, ContextGem, fused tiny local LLMs ; template complet. L'entrée répond explicitement à la question posée (« différence avec un classifieur encodeur ? ») — GLiNER2.5-Decide *est* un encodeur ; le neuf est la calibration + le contrat de type, pas l'archi.

Suggestions (non appliquées) :

- **Synthèse « décider, pas bavarder »** dans `Syntheses/` reliant Needle 2 + decision models + Deja + les expériences confidence-gated/decision.
- Reconduites : back-link Agent Plugins → LLM Wiki ; synthèse « méta-wiki » ; prioriser `exp_kg_relevance_lint.md` (remplacerait l'heuristique d'orphelins de ce health-check).

## Doublons / contradictions

Aucun doublon (une entrée pour la catégorie decision-models + ses 3 incarnations). Aucune contradiction ; complémentarité assumée avec Needle 2 (génération contrainte) vs decision models (pas de génération).

## Note git (rappel d'état)

Ce lint ne committe pas (comportement du skill). Lots non commités qui s'accumulent, à faire côté utilisateur :
ingest LLM Wiki (12-09), lint 12-09, ingest Turbovec (13-09), lint 13-09, passe lab (13-09), ingest decision models (25-09), et ce lint (25-09).
