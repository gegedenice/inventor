# Lint — reindex + health-check — 2026-08-02

## Réconciliation de l'index

`index.md` est **déjà cohérent** avec le contenu réel de `Knowledge/` : **0 entrée ajoutée, 0 orpheline retirée**.
Vérifié — 62 entrées, toutes présentes et sans ligne d'index pointant vers du vide :

- Inspirations : agentic (14), dataviz (2), kb (6), library (3), llm (7), llm-training (2), misc (3), synthetic_data (2).
- Papers (6), Idées (3, incl. `applied_ideas_2026-08-02.md`), Questions ouvertes (2), Signaux faibles (2).
- Expériences (5) et État de l'art (4) — sections ajoutées par la passe `inventor-lab`, correctement reflétées.

Les deux « MANQUE » signalés par le détecteur rapide (*HugginFace moonbot*, *Blockchain…*) sont des **faux positifs**
(différences d'orthographe « Huggin**g** » vs « Huggin » et de format « , » vs « / ») : les deux entrées sont bien indexées.

## Santé du wiki (constats — non corrigés d'office)

### Entrées stub (ni description ni « Why is it interesting »)
- **Markets of agents** (agentic.md) — seulement des liens.
- **Frameworks** (agentic.md) — seulement des liens (ClawCodex, mini-swe-agent).
→ Suggestion : compléter (1 phrase + puces) ou fusionner ; ce sont les deux entrées les plus pauvres.

### « Why is it interesting » absent
- Markets of agents, Frameworks (cf. ci-dessus).
- **SLM extraction de propositions atomiques** (llm.md) — a un Takeaway riche mais pas de puces « why ».
- **AutoData** (synthetic_data.md) — juste un lien arXiv.

### Ligne de description factuelle absente (entrée utilisable, mais template partiel)
Colab CLI, Flue, Dataviz with DeckGL, PageFind, Airweave, QMD, LLM from scratch, VLM trained without image encoder,
World models, SLM pre-training pipeline, HuggingFace ecosystem, Blockchain…, SynthTraces.
→ Mineur : ces entrées ont des puces « why »/Takeaway ; ajouter une phrase d'accroche améliorerait l'homogénéité.

### Entrées orphelines (jamais citées ailleurs en Random Connections)
- **HuggingFace moonbot**, **Markets of agents**, **Frameworks**, **Airweave** — anciennes entrées jamais reliées.
- **OpenWorker** — orpheline *entrante* : elle pointe vers d'autres entrées, mais rien ne pointe encore vers elle.
→ Cross-links sûrs suggérés (à poser lors d'une prochaine passe) :
  - moonbot ↔ **PI agents** (même harnais Pi) et ↔ idée « HF buckets » (mémoire persistante).
  - **Airweave** ↔ *Smart Tool RAG* d'OpenSpace et ↔ idée « RAG sans embeddings » (couche de recherche unifiée).
  - **OpenWorker** ↔ **OpenSpace**/**Orca** (relier depuis ces entrées vers OpenWorker) — trio « interface d'agents ».
  - **Frameworks** ↔ **Flue**/**PI agents** (même famille de harnais).

### Doublons / contradictions
- Aucun doublon actif : les deux doublons rencontrés récemment (**OKF**, **autoarxiv**) ont déjà été *enrichis* et non dupliqués.
- Aucune contradiction factuelle détectée entre notes.

## Bilan
Base saine et bien cross-linkée dans l'ensemble ; la dette se concentre sur **2 stubs** (Markets of agents, Frameworks)
et **4 entrées orphelines** anciennes. Rien de cassé ; index dérivable du contenu réel.
