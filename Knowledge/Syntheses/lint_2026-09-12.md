# Lint — reindex + health-check — 2026-09-12

## Réconciliation de l'index

**6 entrées ajoutées, 0 retirée.** Retard depuis le lint 2026-08-11 (ingests des 08-11 → 09-12 non reportés). Détail :

- **Inspirations** :
  - `agentic.md` — **Agent Plugins** (empaqueter Skill + MCP en capacité ; spec 1.0 ; dépôt Git = marketplace). → fulltext Papers.
  - `llm.md` — **SwarmLLM** (inférence P2P layer-shardée navigateur/WebRTC, moteur WebGPU maison, MTP bit-exact).
  - `kb.md` — **LLM Wiki (nashsu)** (patron Karpathy en app desktop : Ingest/Query/Lint, graphe 4-signaux + Louvain).
- **Papers** : **medium_agent-plugins.md**.
- **Expériences** : **exp_swarm_notices_p2p.md**, **exp_kg_relevance_lint.md**.

Aucune entrée orpheline dans l'index (toute ligne pointe vers un fichier/entrée réel).

## Health-check

Constats principaux :

1. **Dérive d'index récurrente (3e lint de suite)** : `inventor-ingest` n'écrivant pas `index.md` (par design), le catalogue décroche systématiquement entre deux passes. Ce n'est plus un incident mais un *pattern* : soit lancer `inventor-lint` immédiatement après chaque salve d'ingests, soit envisager qu'`inventor-ingest` dépose un TODO d'index dans `log.md` (déjà le cas informellement via « index.md : laissé à inventor-lint »).

2. **Un cluster « Inventor se regarde lui-même » se cristallise** : LLM Wiki (nashsu), Karpathy LLM-wiki, OKF v0.2, Graphify, Agent Plugins, Acontext/SkillOpt/OpenSpace convergent tous vers *l'architecture même d'Inventor* (index/log/wikilinks, Ingest/Query/Lint, skills comme capacités distribuables). La base accumule une matière solide sur son propre modèle opératoire — candidate à une **synthèse transversale** (« méta : bâtir/maintenir un wiki-agent »), et déjà actionnable via `exp_kg_relevance_lint.md` qui vise à muscler `inventor-lint` lui-même.

3. **Cross-links** : les 3 nouvelles entrées sont bien reliées. LLM Wiki → Agent Plugins (distribution comme capacité) et → Karpathy/Deja/OKF/Graphify ; SwarmLLM → AirLLM/Colibri/kimi-k3-in-c (axe orthogonal pooling vs streaming) + Kimi K3 (Gated-DeltaNet). Templates complets pour les trois.

Suggestions (non appliquées — commit indisponible, cf. note) :

- **Back-link Agent Plugins → LLM Wiki** : l'entrée Agent Plugins (antérieure) gagnerait un renvoi vers LLM Wiki, qui *illustre* la thèse (il se distribue via API locale + MCP + `npx skills add`).
- **Synthèse « méta-wiki »** à créer dans `Syntheses/` reliant le cluster du point 2.
- **exp_kg_relevance_lint.md** répond directement au bruit de l'heuristique d'orphelins notée au lint précédent (`lint_2026-08-11.md`) — à prioriser.

## Doublons / contradictions

Aucun doublon de source (LLM Wiki ≠ Karpathy LLM-wiki : app concrète vs patron abstrait, cross-linkés). Aucune contradiction nouvelle.

## Note technique

Passe effectuée pendant une **panne du shell/workspace** (bug lié à la mise à jour Windows du 2026-09-08). Conséquences :
- Réconciliation de `index.md` et rédaction de ce rapport faites via les outils fichier (host-side) — OK.
- **Commit git non effectué.** Restent à committer quand le shell revient : (a) l'ingest LLM Wiki (`kb.md`, `Experiments/exp_kg_relevance_lint.md`, `log.md`) ; (b) ce lint (`index.md`, `Syntheses/lint_2026-09-12.md`, `log.md`).
- Fins de ligne : ce rapport peut être en LF (les autres fichiers de la base sont en CRLF) — à normaliser au commit si besoin.
