# Lint — reindex + health-check — 2026-09-13

## Réconciliation de l'index

**2 entrées ajoutées, 0 retirée.** Un seul ingest depuis le lint 2026-09-12 (Turbovec/TurboQuant, 13-09) :

- **Inspirations** : `kb.md` — **Turbovec + TurboQuant** (index vectoriel frugal + quantization data-oblivious).
- **Expériences** : **exp_turbovec_rag_notices.md** (RAG sémantique frugal/local sur notices, hybride FTS5).

Aucune entrée orpheline dans l'index. Pas de Paper ajouté (Turbovec est une entrée-outil, pas de fulltext).

## Health-check

Constats :

1. **Dérive minime cette fois** : une seule salve non reportée (2 entrées), vite rattrapée. À noter : les skills `inventor-*` ont été mis à jour ce jour pour **ne plus committer** (l'utilisateur gère git) — le contrat « ingest écrit le contenu, lint réconcilie l'index » reste inchangé, seul le commit passe côté utilisateur.
2. **Turbovec bien relié** : l'entrée croise QMD/PageFind/Graphify + le weak signal « pertinence sans vector DB » (elle en est le contre-point frugal), la compression de contexte, Kimi K3 (MXFP4 « ne jamais déquantizer »), ContextGem/LLM Wiki (vector store sous un RAG local) et kimi-k3-in-c/Colibri (kernels bas-niveau CPU). Template complet.
3. **Cluster « frugalité inférence/recherche » dense** : AirLLM, Colibri, kimi-k3-in-c, Needle 2, SwarmLLM, et maintenant TurboQuant convergent dans `StateOfTheArt/inference_archi.md`. La fiche SOTA reste le bon point d'entrée ; rien à scinder pour l'instant.

Suggestions (non appliquées) :

- **Back-link Agent Plugins → LLM Wiki** (déjà noté au lint 09-12, toujours ouvert).
- **Synthèse « méta-wiki »** reliant LLM Wiki / Karpathy / OKF / Graphify / Agent Plugins (cluster « Inventor se regarde lui-même »).
- Prioriser `exp_kg_relevance_lint.md` : il remplacerait l'heuristique d'orphelins de ce health-check par un vrai graphe.

## Doublons / contradictions

Aucun doublon (Turbovec = outil + méthode, une seule entrée ; distinct des autres quantizations de la base, qu'il rejoint sous « ne jamais déquantizer »). Aucune contradiction nouvelle.

## Note git (rappel d'état)

Ce lint **ne committe pas** (nouveau comportement du skill). Restent non commités, à faire côté utilisateur quand le shell/git remarche :
- ingest LLM Wiki (12-09) ;
- lint 12-09 (`index.md` + `Syntheses/lint_2026-09-12.md` + `log.md`) ;
- ingest Turbovec/TurboQuant (13-09) ;
- ce lint 13-09 (`index.md` + `Syntheses/lint_2026-09-13.md` + `log.md`).
